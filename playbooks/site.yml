---
- hosts: docker_compose
  gather_facts: yes
  become: yes
  tasks:
    - name: Ensure Docker is installed
      import_role:
        name: docker
      tags:
        - always
        - docker

    - name: Generate docker-compose deployment files
      import_role:
        name: la-compose
