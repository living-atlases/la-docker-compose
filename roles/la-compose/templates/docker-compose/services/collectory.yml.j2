services:
  # Collectory service
  collectory:
    container_name: la_collectory
    profiles: [app-core, full]
    extra_hosts: {{ docker_extra_hosts_dict | default({}) | to_json }}
    image: {{ docker_registry | default('') }}{% if docker_registry %}/{% endif %}collectory:{{ collectory_version | default('latest') }}
    volumes:
      - {{ data_dir }}/collectory:/data/collectory
      # Bind-mount /var/log/tomcat9 because the bundled logback.xml hard-sets
      # LOG_DIR via <variable> (overriding env vars). Mounting the host logs
      # dir lets logback write where it expects.
      - {{ data_dir }}/collectory/logs:/var/log/tomcat9
{% include '_i18n_volumes.yml.j2' %}
    environment:
      TZ: {{ server_tz | default('UTC') }}
      JAVA_OPTS: ${COLLECTORY_JAVA_OPTS}
    expose:
      - 8080
    networks:
      default: {}
      {{ docker_network | default('internal') }}:
    depends_on:
{% if i18n.enabled and i18n.strategy == 'volume' and i18n.volume.source == 'init-container' %}
      i18n-init:
        condition: service_completed_successfully
{% endif %}
{% if enable_mysql | default(false) | bool %}
      mysql:
        condition: service_healthy
{% endif %}
{% if 'cas' in services_enabled %}
      cas:
        condition: service_healthy
      userdetails:
        condition: service_healthy
      apikey:
        condition: service_healthy
      cas-management:
        condition: service_healthy
{% endif %}
    healthcheck:
      # Does not have health activated
      # test: ["CMD", "sh", "-c", "curl --fail --silent http://localhost:8080/actuator/health || exit 1"]
      test: ["CMD", "sh", "-c", "curl --fail --silent http://localhost:8080/ || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    restart: unless-stopped

